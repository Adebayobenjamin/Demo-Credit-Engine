version: "3.2"

services:
  db:
    image: mysql:8.0
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: thtxyyx
      MYSQL_PASSWORD: rtyyxksl
      MYSQL_DATABASE: democredit
      MYSQL_USER: democredit
    ports:
      - 3306:3306
  server:
    build: .
    # depends_on:
    #   - db
    environment:
      - PORT=9898

    ports:
      - 9898:9898

volumes:
  db:
    driver: local
